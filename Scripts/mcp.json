{ 
   "inputs":[
        {
            "id": "mssql-server-mcp-api-key",
            "type": "promptString",
            "description": "Enter your SQL Server MCP API Key (Master Key for full access or Managed Key for limited access)",
            "password": true        
        }
   ],
   "servers": {
        "mssqlmcp": {
            "url": "http://localhost:3001",
            "headers": {
                "Authorization": "Bearer ${input:mssql-server-mcp-api-key}",
                "Content-Type": "application/json"
            },
            "description": "SQL Server MCP with 22 tools including API key management. Supports both master keys (full access) and managed keys (granular permissions)."
        }
    }
}